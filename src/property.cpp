#include <iostream>

#include "input.hpp"
#include "options.hpp"

void weak_immunity(const Options &options, const Input &input) {
	std::cout << "Ivana, this is your moment!" << std::endl;

	// I'd sketch out a procedure for weak immunity first.
	// You might find the methods in `input.hpp` (e.g. mark_honest(), reset_reason()), helpful.
	// Case splitting etc can come later.
	// Then maybe we can factor out the common logic for other properties, like case splits.
}
